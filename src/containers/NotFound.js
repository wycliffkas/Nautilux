import React from "react";

const NotFound = () => {
	return (
		<div>
			<h4 style={styles.title}>404 Page Not Found!</h4>
		</div>
	);
};

export default NotFound;

const styles = {
	title: {
		marginTop: "20px",
		textAlign: "center"
	}
};
